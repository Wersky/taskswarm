# 运行期状态落盘目录（每个工作区一份，含任务树与看板进度）
任务蜂群/

# 依赖（本插件零依赖，此处仅防御性忽略）
node_modules/

# 原子写临时文件（server.mjs 写状态时产生的中间文件）
*.tmp-*
*.corrupt-*.json

# 子代理协作临时目录 / 锁文件
_swarm-tmp/
.lock

# 编辑器与系统噪音
.vscode/
.idea/
.DS_Store
Thumbs.db
*.log
